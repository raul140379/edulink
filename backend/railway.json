{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx prisma migrate deploy && npx ts-node src/index.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}